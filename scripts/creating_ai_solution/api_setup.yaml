# 0. 로그인
'STATIC_LOGIN': 'api/v1/auth/static/login' # POST
'LDAP_LOGIN': 'api/v1/auth/ldap/login'

# 1. 시스템 정보 획득
'SYSTEM_INFO': 'api/v1/workspaces' # GET

# 2. AI Solution 이름 설정 / 3. AI Solution 등록
'SOLUTION_LIST': 'api/v1/solutions/workspace' # 이름 설정 시 GET, 등록 시 POST

# 3. AI Solution 등록
'REGISTER_SOLUTION': 'api/v1/solutions' # 등록 시 POST

# 4. AI Solution Instance 등록
'SOLUTION_INSTANCE': 'api/v1/instances' # POST

# 5. Stream 등록
'STREAMS': 'api/v1/streams' # POST

# 5. Stream 등록
'STREAM_RUN': 'api/v1/streamhistories' # POST

# REST API Endpoints
# 6. Train pipeline 요청
# STREAMS + '/{stream_id}/start # POST
# 7. Train pipeline 확인
# STREAMS + '/{stream_history_id}/info # GET
# 9.a Stream 삭제 
# STREAMS + '/{stream_id} # DELETE
# 9.b AI Solution Instance 삭제
# SOLUTION_INSTANCES + '/{instance_id}' # DELETE
# 9.c AI Solution 삭제
# AI_SOLUTION + '/{solution_id}' # DELETE 